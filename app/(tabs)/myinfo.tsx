import { AppWebView } from '@/components/AppWebView'

/**
 * 내정보 탭 · 자소서 소재 · 학력 · 자격증 등 (chwippo-front `/myinfo`).
 */
export default function MyInfoScreen() {
  return <AppWebView path="/myinfo" />
}
